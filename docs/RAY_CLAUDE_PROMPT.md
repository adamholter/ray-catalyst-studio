# Prompt For Ray To Paste Into Claude

Replace the bracketed placeholder with your fal.ai API key before giving this prompt to Claude. The repository is public; credentials and generated data must stay private.

```text
Clone https://github.com/adamholter/ray-catalyst-studio, read the README and the docs in the repo first, then follow those instructions to get Catalyst Studio running locally.

The API key to set up is [PASTE_FAL_KEY_HERE]. Put it only in the local .env file as FAL_KEY, set CATALYST_PROVIDER_MODE=live and CATALYST_LLM_PROVIDER=fal-openrouter. Follow the repository's setup and verification instructions. Do not commit the .env file or print the key back to me.

When it is running, give me the local URL and a short explanation of what I need to know. If you make code changes, use a branch and do not push to main directly.
```
